#!/usr/bin/env bash
# This script displays all active IPv4 addresses using ifconfig
# to ensure compatibility with environments where 'ip' command is missing

ifconfig | grep -oP '(?<=inet\s)\d+(\.\d+){3}'
